# @react-atom/atom-styled-components

> Build your application atomic with your design! (base on styled-components)

## Install

Using npm:

```sh
npm install @react-atom/atom-styled-components
```

or using yarn:

```sh
yarn add @react-atom/atom-styled-components
```
