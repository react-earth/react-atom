{
  "name": "react-atom",
  "version": "independent",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/react-atom/react-atom.git"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "watch": "lerna run watch --stream",
    "build": "lerna run build",
    "version": "lerna version --conventional-commits --create-release github --yes --message \"chore: publish packages\"",
    "examples": "yarn --cwd examples start"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
